dados <- read.csv ("ExperimentosME110.csv", header=TRUE)

head(dados)

```{r}
1. Considerando o Experimento 1:
1.1. Qual é o espaço amostral desse experimento?
1.2. Qual a probabilidade de sucesso? Existe um modelo teórico de probabilidade chamado modelo
Binomial. Para este modelo devemos conhecer o número de ensaios e a probabilidade de sucesso. Como
você definiria o modelo Binomial para esse experimento?
1.3. Quais suposições devem ser satisfeitas para os ensaios serem válidos?

2. Seja X uma variável aleatória que representa o número de valores pares em n lançamentos. Assuma
que X tem distribuição Binomial definida a partir das características do Experimento 1.
2.1. Quais os possíveis valores de X?
2.2. Calcule a probabilidade de X ser igual a
{np − 5, np − 4, np − 3, np − 2, np − 1, np, np + 1, np + 2, np + 3, np + 4, np + 5}
a partir do modelo Binomial, onde n é o número de lançamentos e p a probabilidade de sucesso.
2.3. Calcule as mesmas probabilidades do item 2.2, mas agora a partir dos dados observados.
2.4. Considerando os itens 2.2 e 2.3, você acha que as probabilidades observadas justificam o modelo
Binomial como apropriado para estes dados? Justifique.
2.5. Quais suposições devem ser satisfeitas para que os ensaios/lançamentos sejam válidos?
```

